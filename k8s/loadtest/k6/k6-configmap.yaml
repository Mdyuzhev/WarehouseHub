# ConfigMap для k6 тестовых сценариев
# WH-209 (сценарии будут добавлены в WH-210, WH-211)
apiVersion: v1
kind: ConfigMap
metadata:
  name: k6-scripts
  namespace: loadtest
data:
  # Placeholder - реальные сценарии добавятся позже
  placeholder.js: |
    import { check, sleep } from 'k6';

    export const options = {
      vus: 1,
      duration: '10s',
    };

    export default function() {
      console.log('k6 is working!');
      sleep(1);
    }
