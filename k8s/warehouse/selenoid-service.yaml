# =============================================================================
# Selenoid Service
# =============================================================================

apiVersion: v1
kind: Service
metadata:
  name: selenoid
  namespace: warehouse
  labels:
    app: selenoid
    component: testing
spec:
  type: ClusterIP
  ports:
    - port: 4444
      targetPort: 4444
      name: http
  selector:
    app: selenoid
